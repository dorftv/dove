# Debian Trixie overlay — uses Dockerfile.trixie instead of Alpine
# Usage: docker compose -f docker-compose.yml -f docker-compose.trixie.yml -f docker-compose.local.yml up
services:
  dove:
    build:
      context: .
      dockerfile: Dockerfile.trixie

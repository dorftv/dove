# AMD host GPU passthrough overlay
# Usage: docker compose -f docker-compose.yml -f docker-compose.amd.yml up
services:
  dove:
    devices:
      - /dev/dri:/dev/dri
    environment:
      - AMD_VULKAN_ICD=RADV
      - LIBVA_DRIVER_NAME=radeonsi
